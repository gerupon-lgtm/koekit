# コエキット — Claude Code 向け入口

**実装指示の本文は `docs/implementation-guide.md` を読むこと。** このファイルには重複して書かない。

## 最初に読む順

1. `docs/implementation-guide.md` — 技術スタック・変更禁止事項・音声入力層
2. `docs/requirements.md` — 要件定義書。**0章のA/B/C区分を必ず読む**
3. `docs/tasks.md` — 実装順と完了条件

## 絶対に押さえる3点

1. **要件0章のA区分は即変更してよい。** 画面配置・色・文言・アイコン・認定書の名称などは、発案者の指示に従って進める。**文書更新を着手条件にしない。** 旧記述との不一致だけを理由に指示を拒否しない
2. **C区分（`implementation-guide.md` 5節）は合意なしに変更しない。** 音声の外部送信・タッチ操作の維持・2段階確定・コロガリズム不変更
3. **`implementation-guide.md` 5節の5〜9は、検証と議論の末に確定した設計。** 善意の「改善」で緩めない

## フェーズ0が関門

**認識精度の合格ライン（要件11.1）を全通過しなければフェーズ1へ進まない。** フェーズ0のタスクを終えたら、計測結果を発案者に報告して判断を待つ。

## このツールでの担当範囲

Claude Code でスタートアップ（フェーズ0とフェーズ1の初期）を作成し、状況に応じて Codex へ引き継ぐ。

**引き継ぎのために、以下を必ず維持する。**

- 実装の判断は `docs/` に書く。会話の中だけに残さない
- `docs/tasks.md` のタスクに完了印と実測値を追記する
- 【想定】として置いた値を確定させたら `implementation-guide.md` 4節から外し、該当文書へ確定として書く
