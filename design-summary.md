# 基本設計サマリ：どうぶつめくり（仮称）／コエキット

| 項目 | 内容 |
|---|---|
| 版数 | 1.0 |
| 作成日 | 2026-09-16（JST） |
| 作成者 | たんたん（シクミラボ / SIKUMI LAB） |
| 入力 | 要件定義書_どうぶつめくり.md（版数1.0） |
| モード | 新規モード |

## 1. 全体構成

```
利用者の端末（Android / Chrome）
├── GitHub Pages : gerupon-lgtm.github.io/koekit/
│     ├─ コエキット メインページ
│     └─ どうぶつめくり（素のHTML/CSS/JS、PWA）
│          ├─ 音声入力層（方式A/B/C を差し替え）
│          ├─ 区間ステートマシン
│          ├─ ログ記録（メモリのみ）→ CSVをクリップボードへ
│          └─ Cache Storage（アプリシェル・フォント・素材・Voskモデル）
└── 別ホスティング（【想定】Cloudflare）: Voskモデル約48MB
```

**サーバーサイドの処理を持たない。** APIが存在しないため `docs/api-design.md` は作成していない。外部との通信はVoskモデルの取得のみ。

## 2. 技術選定と根拠

| 項目 | 選定 | 根拠 |
|---|---|---|
| 言語・構成 | 素のHTML/CSS/JS、ES Modules、**ビルド工程なし** | 軽量なツールを好むという方針。依存が増えると引き継ぎ時の負担になる |
| ホスティング（アプリ） | GitHub Pages | コロガリズムと同じ。HTTPS必須要件を満たす |
| ホスティング（モデル） | **【想定】** Cloudflare（R2/Pages） | 48MBをリポジトリに入れない。CORSヘッダを設定できることが条件 |
| データベース | **なし** | 永続化する要件がない（ログはその回限り、進捗は保存しない） |
| フォント | M PLUS Rounded 1c を **woff2 で自前配置・サブセット化** | オフライン対応のためCDNを使わない。使う文字が限られるためサブセットが効く |
| アイコン | **ライブラリを使わずインラインSVG**【想定】 | 画面に文字を使わないため必要な図形が少ない。R-06の決定後に見直す |
| 音声認識 | 入力層を抽象化し **方式A/B/C を差し替え可能に** | 採用方式が実測後に決まる（R-02）。後続試作への共通部品にもなる |
| 認証 | なし | 誰でもアクセスできる公開作品 |
| テスト | 実機確認＋Nodeスクリプトによるロジック検証 | 答えが一意に決まる処理（語彙照合・CSV・判定・バージョン同値）はスクリプト化 |

## 3. 設計の核：音声入力層の抽象化

**この案件の技術的な核は、方式を差し替えられることにある。** 採用方式がフェーズ0の実測後に決まるため、ゲーム側のコードが方式を知らない構造にする。

```
game/phase.js  ──(受け付ける語を渡す)──▶  speech/index.js
                                              ├─ webspeech.js       方式A（検証専用）
                                              ├─ webspeech.js       方式B（processLocally）
                                              └─ vosk.js            方式C（本命）
```

副産物として、**同一条件での方式比較が可能になる**（同じゲームで方式だけを切り替えて計測できる）。またコエキットの後続試作へそのまま持ち込める。

## 4. 区間ステートマシンが同時に解決している問題

**受け付け区間ごとに認識を開始・停止する設計**（常時認識にしない）は、3つの問題を一度に解いている。**このため常時認識へ変更すると3つとも復活する。**

1. **鳴き声との干渉** — 結果表示中は認識が停止しているため、アプリ自身が鳴らす音を拾わない
2. **区間外の語の誤認識** — 位置語待ちで「スタート」を受け付けないため、似た音の誤判定が減る
3. **話していい状態の可視化** — 区間が画面表示と1対1で対応するため、文字を使わずに伝えられる

## 5. 検証済みの事実

**現時点ではなし。** フェーズ0の計測結果を `docs/tasks.md` の「検証済みの事実」に追記する。

## 6. 残る【要確認】事項

| ID | 項目 | 設計影響 | 次の判断 |
|---|---|---|---|
| **T-004** | **vosk-browser がクロスオリジン分離を必要とするか** | **大。必要ならGitHub Pagesにヘッダを付けられないため `mini-coi.js` 等の設置が必要になり、さらにモデル配信元のCORS設定が必須になる** | フェーズ0の最初に調査 |
| R-02 | 音声認識方式の採用（本命C、代替B） | 大（入力層の実装に影響）。抽象化により吸収済み | フェーズ0の実測 |
| R-01 | 正式名称 | 中。**アプリのディレクトリ名 `doubutsu/` を【想定】で置いている。公開前に確定が必要** | 発案者 |
| R-03 | オフライン対応の要否 | 中。方式CはCache Storageで制御できるが、方式Bの言語リソースはブラウザ管理下 | 方式決定後 |
| R-06 | アイコンをSIKUMI LAB他作品と揃えるか | 小。T-030で実画面を見て決める | 正式名称の決定後 |
| R-07 | 色覚・音量への配慮 | 小。矢印は色だけで識別させない設計にしてある | 発案者 |
| R-04 | 公開の判断基準 | 小（実装に影響しない） | 発案者 |
| R-05 | 動物の種類数 | なし（A区分。実装時に決定） | 実装時 |
| R-08 | 要件定義の単位（コエキット文書を別立てするか） | なし | 後続試作の着手時 |

## 7. 特記事項

**vosk-browser の保守状況。** 最終公開が4年前（v0.0.8）で更新が止まっている。本命方式の土台であるため、T-008で早期に動作を確認する。動作しない場合は方式Bへの比重の移動、またはフォーク・別ライブラリの検討が必要になる。

**フェーズ0が明確な関門。** 合格ライン（要件11.1の4指標）を全通過しなければフェーズ1へ進まない。**閾値は実測前の仮置きであり、要件0章のB区分として実測後に変更してよい**（要件11.1の更新＋改訂履歴1行の追記で足りる。合意のやり直しは不要）。

**実装AIの引き継ぎ。** Claude Code で着手し、状況に応じて Codex へ引き継ぐ。共通本文を `docs/implementation-guide.md` に一本化し、`CLAUDE.md` と `AGENTS.md` は入口のみとした。**判断を会話に残さず `docs/` に書くことを両入口に明記している。**

## 8. 成果物一覧

| ファイル | 内容 |
|---|---|
| `CLAUDE.md` | Claude Code 向け入口 |
| `AGENTS.md` | Codex / ChatGPT 向け入口 |
| `docs/implementation-guide.md` | **実装指示の本文（正典）。** 技術スタック・変更禁止事項・音声入力層・日時・バージョン管理 |
| `docs/requirements.md` | 要件定義書（版数1.0）のコピー |
| `docs/screens.md` | 画面一覧・遷移・遷移完全性・デザイントークン |
| `docs/data-model.md` | 永続化方針・ログ構造・CSV書式・レベル定義 |
| `docs/tasks.md` | T-001〜T-031・完了条件・検証方法・トレーサビリティ表・主要異常系 |
| `基本設計サマリ.md` | 本ファイル |

`docs/api-design.md` は**作成しない**（サーバーAPIが存在しないため）。
