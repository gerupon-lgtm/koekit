# 引き継ぎ（Claude Code → Codex）

## 2026-09-20 4作品のメニュー配置を承認案へ反映

ロゴ拡大と上方移動、4作品のロゴ・メニュー・フッター位置を統一。基準412×840の位置は上から58px／229px／フッター下端832px。ピタリ・メモリの称号カードは下端から64pxで固定。イロドリの書きかけボタンも考慮し、ジントリの説明をロゴ下の「こえで、じんとりしょうぶ。」へ移動。小さい画面はコンパクト表示。詳細・検証は `docs/menu-fit-review.md` 冒頭を参照。

## 2026-09-20 メニューのホーム・ヘルプを統一

4作品のホームはピタリズムの塗りつぶし家アイコン、ヘルプはイロドリズムの「？」へ統一。共通意匠は `src/ui/menu-controls.css`。ピタリズム・メモリズム（場所／順番）には `src/ui/menu-help.js` で遊び方ダイアログを追加。ピタリズムの計測パネル入口はヘルプ内へ移動し、機能を維持。ホーム遷移・既存2作品のヘルプ処理は維持。

ロゴ位置とメニューボタンの配置は今回変更しない。次は発案者とピタリズムのロゴを下げる案・ボタン配置を相談し、その後メモリズムの位置合わせ可否を検討する。ホーム共通描画・5画面のヘルプ開閉・計測パネル到達をChromeで確認。既存の3モード×6画面サイズの配置一致検証も成功。

## 2026-09-20 トップの4作品を一画面に調整

実機写真の指摘に合わせ、トップのコエキットロゴとキャッチコピーの間隔を短縮。4作品のカード高と余白を画面高に応じて調整し、家族向け案内・コピーライトまで表示する。文字拡大や横向きでは内容を隠さず自然にスクロールする。ゲーム画面のレイアウトは変更しない。

## 2026-09-20 ジントリズムのローカル実装

発案者の「確認が必要になるまで合意済み項目に沿って進める」に基づき `jintori/` を追加。通常対戦・強化/最強・最多同数の番号選択・色選択・サイコロ先攻・規定局数/延長・CPU連勝・難易度3段階・保存再開・正式終了まで接続。トップ、共通ズム部品のロゴ、SW事前キャッシュ、版検証にも追加した。

設計と確定仕様は [基本設計v0.3](jintorhythm-design.md)、実装範囲・再実行手順・結果は [実装記録](jintori-implementation-log.md)。新規Node56ケース、Chrome/WebKit基本操作、ChromeのWorker/競合/容量不足/オフライン、既存3作品回帰が成功。実発話・Android/iOS PWA受入は未実施。公開状況は次段落を参照。実装前から存在する `src/speech/vosk.js` の未コミット変更は触っていない。

後続：発案者の公開承認を受け、2026-09-20に `https://koekit.sikumilab.com/jintori/` へ公開（初回コミット `2dd1cd6`）。効果音を9種類へ拡張し、固定4×4では「毎局補充／持ち越し」を非活性化。HTTPS配信と公開版の基本操作を確認。実機発話・PWA受入のみ発案者確認待ち。詳細は実装記録を参照。

## 2026-09-20 連続開始の入口を復旧（発案者承認）

メニュー整理で再生ボタンを外したのはデグレード。入口を重複とみなして削除しない。
ピタリズム・メモリズム場所・順番すべてを、同じ画面の「▶ はじめから あそぶ → 通常の各レベル5個 → スピード」の7ボタンに統一。レベル表示は場所名／2て〜6てであり、番号に置換しない。

- はじめから：ピタリズムは練習Lv0からLv1へ、メモリズムは通常Lv1から。クリア後の「つぎ」で順に進行する既存処理を維持。
- 通常の各レベルは直接開始でき、従来どおりクリア後に次へ進める。
- スピードはその作品・モードの通常Lv1〜5すべての保存済みクリアで解放。未解放は鍵と条件表示。独立した1入口のみ、途中のスピードレベル選択は設けない。順番はs1からs5へ連続進行、場所系は既存extraを開始。
- 通常／スピードの切り替えタブを撤去。メモリズムの場所／順番は残す。「順番をいったん外す」は議論対象を2つに絞る意味であり、機能撤去の指示ではない。
- 承認されたブラウザ描画案を使用。390×844はボタン56px・間隔16px、390×664は44px・10px。320×568は44pxを維持して間隔7px。ロゴ・開始ボタン・称号位置を3画面で揃え、フッターは一画面の下部。
- プレビューも共通の描画関数と承認CSSを使用。ゲーム記録を変更しない。

検証：本番とプレビューの位置・寸法一致（3モード×6サイズ）、7ボタンの文字収まり、フッター余白、通常の次レベル遷移、順番スピードs1〜s5通しクリア、ロックと全通常クリア解放、記録保持、オフラインをブラウザ自動検証。実機の音声認識は今回の検証対象外。

## 2026-09-20 メモリズムにも承認済みレイアウトを適用

ピタリズムの実機確認でユーザーからOKと継続指示を受領。メモリズムの場所・順番にも同じ `installMenuLayout` を接続。ロゴ・ホーム位置・文字サイズを維持し、称号上の12px追加余白と画面高への調整を共通化。スピード解放の案内も対象が分かる文面に揃える。プレビューの「現在の公開画面」も全モードで同じ承認済み処理を再現。

本番と承認プレビューの照合テストを3モード×4サイズへ拡張。順番ゲーム進行、ロック／解放、オフライン表示も検証。

## 2026-09-20 実画面で承認されたメニューをピタリズムへ適用

ユーザーが3モードの実画面比較を承認。390×844pxは受領値から選択ボタン高のみ70→60px、称号上の追加余白12pxで一画面に収まる。小さい画面は `menu-fit-review.md` の順序・下限で調整。

本番とプレビューのCSS生成・画面収容処理を `src/ui/menu-layout.js` / `menu-fit.js` に統一し、ピタリズムでのみ起動する。`test-approved-menu-browser.cjs` で本番画面と承認済みプレビューの各要素の位置・寸法・文言が4サイズで一致することを確認。共通モジュールはSWの事前キャッシュへ追加。

メモリズム本体の見た目は変更しない。ピタリズムの実機OK後に同じ処理を適用する。

## 2026-09-20 メニュー調整プレビュー

先行公開したC案はユーザーの意図と一致せず、本番の追加変更を停止。今後は生成画像ではなく `/menu-preview/` の実画面を同じ数値で比較する。ユーザーの依頼で独立した調整ページを追加。高さ・間隔・ロゴを即時変更し、設定をコピー／共有できる。ゲーム本体の見た目・記録は変更しない。詳細は `menu-preview.md`。

**ユーザーが実機で選んだ設定を受け取るまで、本番のメニューへ適用しない。** ピタリズムとメモリズムを同じ設定で確認し、公開はピタリズム先行、実機OK後にメモリズムとする。

## 2026-09-20 ピタリズムのC案を先行適用

ユーザーが3案からCを選択。「れんしゅう」と通常／スピードは高さ36px、上部の間隔8px、5つの位置ボタンは均等な高さ（最大64px）と12px間隔に調整。ロゴは現行画像を使い、上部の位置を8pxだけ下げる。1画面表示と通常／スピードでの記録位置を維持。

変更は `.doubutsu` のCSSに限定。**メモリズムは実機でユーザーのOKが出るまで変更しない。** 既存のメニュー検証に均等なボタン高・12px間隔・上部36pxの検証を追加。

## 2026-09-20 両作品のメニュー（合意B案・1画面配置）

場所・順番とも、イロドリズムを参考に大きな横長ボタンと「通常／スピード」の切り替えに統一。順番は「2て〜6て」、場所は位置の名前を表示し、選択ボタンのレベル番号を除去。未解放のタブは「🔒 スピード」で選択不可、通常全クリアで解放。クリア済みの✓・最高称号・保存済み記録は維持。

ピタリズムにも同じ通常／スピード切り替えと位置名の横長ボタンを適用。「れんしゅう」はロゴ下の独立した横長ボタンとして維持し、従来のレベル0へ進む。計測パネルも維持。

現行ロゴ画像と色味を維持。共通の上端配置と5ボタン分の領域により、両作品・場所／順番・通常／スピードでロゴ・記録の表示位置を揃える。ユーザー指示によりメニューを100svh内へ配分し、ボタン高・余白・ロゴ領域を画面高に合わせて調整。スマホ縦画面では記録・フッターまで縦スクロールなしで表示する。メモリズムは重複する開始ボタンを除去し、内容ボタンから開始。

検証：`test-memory-menu-browser.cjs` で320×568、360×640、390×664、390×844、768×1024の配置一致、縦横スクロールなし、文字の収まり、ロック／解放、キーボード切り替え、クリア表示を確認。通常・スピードで記録位置も一致。既存progression/sequenceブラウザテストでゲーム進行、ピタリズムの練習、両作品の解放後スピード入口を確認。

## 2026-09-20 音声開始失敗の表示

両作品・順番のstartListeningで受付表示を先に設定し、同期的な開始失敗で出した斜線マイクを受付中表示で上書きしない。モデル初期化／認識器生成／言語未対応のエラーもタッチ継続表示へ移す。マイクのaria-labelで「音声を使えません。タッチで操作できます」と案内。Voskアダプタ自身の既存未コミット差分は変更しない。

検証：B非対応時の表示不具合をブラウザで再現して修正。両作品・順番のモデル初期化失敗を模擬し、斜線表示とタッチ操作を確認。既存171件・順番ブラウザ・オフラインも通過。


## 2026-09-20 T-026 バージョン同値検証

未配置だった `scripts/check-version.cjs` を追加。version.json、manifest、SWのVERSION/BUILD/APP_CACHE、両作品のタイトルフッタを比較。タイトル以外にフッタを置かない構造と著作権表記も確認する。独立して版管理されているイロドリズムの版は変更しない。`test-version.cjs` は各反映先の不一致・BUILDのみ不一致・フッタ欠落・壊れたJSONを検出する。

実装と判断待ちの整理は `docs/implementation-audit-2026-09-20.md` を参照。


## 2026-09-20 オフライン用資産とキャッシュ保持

SWは全動物11枚とVoskライブラリをSTATIC_CACHEへ事前保存（既存資産は再取得しない）。AudioWorkletとSW登録スクリプトもシェルに追加。activateで削除するのはkoekit-app-/koekit-static-の旧版のみとし、Voskモデルや他アプリのCache Storageは保持。

`test-sw.cjs` でキャッシュ所有範囲・動物マニフェストとの一致を検証。`test-offline-browser.cjs` は手動の画像取得を取り除き、SWの事前保存だけで全画像と順番のタッチ回答を確認。実機の機内モードとモデル取得済み音声認識は未検証。

設定画面は必要になった時に実装する方針を維持。将来は点灯時間を通常×0.75でスピードへ連動。未接続の設定試作は `.local-tools/sequence-settings-draft/` に保管（Git／公開対象外）。


## 2026-09-20 T-029 公開ゲームの音声方式制限

方式Aを計測パネルから除外。両作品のURL／旧保存設定のA・未知値はVoskへ正規化し、共有factoryでもAを拒否。方式BはprocessLocallyが存在してtrueを設定できる場合だけ開始し、非対応ならタッチで継続。共通音声層の差し替えインターフェースとVosk実装は維持。検証専用WebSpeechAdapterとprobeは残すが公開ゲームからAへ切り替える入口はない。

検証：speech-policy Node、既存171件、Chromeで両作品×URL A／未知／B、旧A保存設定、B非対応時の認識開始0回・タッチ継続を確認。ブラウザテストではモデル取得を止め、実発話は使用していない。


## 2026-09-20 メモリズム・順番

場所／順番の入口を追加。順番は9マス固定、2〜6手、通常800/400ms・スピード600/300ms、3問中2正解。反復回答・末尾取消・全消去・全体確定・番号付き手動答え合わせ・モード別記録と最高称号を実装。設定画面は合意どおり後続（最大9手と速度設定用のロジックは分離）。陣地対戦は未着手。詳細・再実行手順は `docs/sequence-mode.md`。

検証：既存Node171件、追加progress/sequence、イロドリズムのtemplates/tutorial、Chromeの両作品回帰と順番の模擬音声／タッチ結合、取得済み画像でのオフライン・マイクなしタッチが通過。320pxで案内全体と結果の次へが画面内。実発話・実機PWAは未確認。

第1段階の公開：`3567347`、GitHub Pages run `35473832962` 成功、公開4主要ファイルの一致を確認。現行公開URLは https://koekit.sikumilab.com/ 。第2段階の公開：`3a73a5a`、Pages run `35474574379` 成功、公開14ファイルの一致を確認済み。


## 2026-09-20 クリア記録・称号

ピタリズム／メモリズム・場所の通常Lv1〜5クリアを端末内保存。スピードは既存extraを維持し、全通常クリアで解放。各通常レベルのメダル＋称号、スピードマスター、タイトル最高称号、選択のクリア印とロック、プレイ中のレベル数字を実装。未クリア結果の次へは同レベル再挑戦、終了はタイトル。過去の未保存プレイは補完しない。保存拒否でも遊べて、セッション限定の旨を表示する。

検証：既存Node 171件、progressロジック、Chromeで両作品のタッチクリア・保存復元・ロック・320/390/768px表示。音声入力は模擬で、実発話・実機PWAは未確認。`docs/progression-plan.md` を参照。


## 2026-09-20 ピタリズムの不正解表示

発案者指定でメモリズムと統一。不正解時は全札を開き、動物は正解位置1枚だけに表示（他は空白）。選んだ誤答のみ赤枠＋×、正解位置1枚のみ緑枠＋チェック。選択中・停止位置の印は解除する。表示時間はメモリズム同様2.8秒。次試行では画像と正誤マークを消去。正解時の中央拡大演出は維持。

## 2026-09-19 動物画像の透過版への差し替え

- 発案者が `assets/animals/` の11枚を同名の透過PNGへ差し替え。全画像がアルファ付き、四隅は透明。monakaのみ675×675、他10枚は632×632で、すべて正方形。
- `STATIC_CACHE` を `koekit-static-v2` へ更新し、旧画像キャッシュを更新対象にした。元画像の再加工は行っていない。

## 2026-09-19 正誤表示・WebKitの正解カード位置

- 発案者がiOSで音声認識できることを確認（機種・OS版・PWA起動は未記録）。音声層の変更は行っていない。
- メモリズムの確定時は選択中の印を解除。不正解の札は赤枠＋×、正解の札のみ緑枠＋チェック。全札開示・2.8秒の表示は維持し、次の試行で両方の印を解除する。
- 両作品の `CardReveal`：割合指定のGrid内で画像の自然幅に影響されていた配置を、ステージ内寸で決める正方形＋Flex中央配置に変更。画像は枠とは別の内側コンテナへ収め、角で枠を覆わないようにした。
- Windows上のWebKit 26.6で旧表示を再現。390×844で横方向に約84.7pxのずれ。修正後は320×568／390×844／768×1024で中心差0.02px未満、画像が枠の内側に収まる。iPhone実機での見た目の再確認は別途。
- 検証：Nodeロジック171件通過。ChromeとWebKitでメモリズムの不正解→次試行の印解除、両作品の正解拡大→音声「つぎ」で解除を確認。検証時の認識入力は模擬。
- 動物PNG全11枚は632×632・RGB（アルファなし）。薄い背景／市松模様は元画像に含まれており、今回は画像ファイル自体を加工していない。
- イロドリズムは `docs/irodori/handoff.md` を確認のみ。v0.2.6、保存・再編集・範囲・線・Undo/Redo等が実装済み。見本・ヒント・保護者intro・トップ/SW統合は未実装として引き継ぐ。コード・仕様は変更していない。

Claude Code が着手した部分の到達点と、続きに必要な情報をまとめる。**まず `AGENTS.md` → `docs/implementation-guide.md` → `docs/requirements.md`（0章A/B/C）→ `docs/tasks.md` を読むこと。** 本書はその上での現状スナップショット（最終更新 2026-09-18、コミット `6977aef`）。

---

## 0. 1分サマリ

### レベル境界の音声・正解札の演出（2026-09-18）

- 紹介の位置語は枠との接触を避けるため13〜18px（360px幅では14.4px）へ再調整し、カード内側に8pxの余白を確保。2026-09-19にスクロール不要の配置へ更新。本文14〜15px、手順14〜15px、補足13〜14px。説明カードの外に104px高の開始ボタンを配置。

- 両作品：紹介を「オーケー／オッケー」、認定書を「つぎ」で操作。紹介は大きなチェック（104px高）、認定書は次へボタン（最小136×112px）。小画面でも説明とチェックを一画面に表示（縦スクロールなし）。
- 受付は `await_intro` / `await_next`。効果音後750msから再開し、画面移動時は予約を解除。共有制御は `src/ui/levelnavigation.js`。ゲームオーバー・延長クリア後の「つぎ」はタイトルへ戻る。
- ピタリズム：正解札の動物を中央へ460msで拡大。結果表示は実機フィードバックで1500ms→3000msに延長（不正解1800msは維持）。動きを減らす設定では移動なし。`src/ui/cardreveal.js`。
- Vosk：初期化待ちの共有・停止後の遅延結果破棄を追加。紹介をタッチで閉じても古い区間の認識を再開しない。
- 検証：ロジック139件通過。Chromeで音声結果を模擬し、両作品のクリア→次レベル→紹介確定、重複語の無視、マイク拒否時のタッチ、390×844／360×640表示を確認。全13紹介のボタンが画面内に収まること、不正解時は拡大しないこと、reduced-motion、演出途中の終了も確認。音声の実発話・Pixel 6aでの新語「つぎ」の精度は未測定。

### CodexでのUI更新（2026-09-18）

- ロゴ：コエキットのアイコンと、小品の文字ロゴを制作。**ピタリ／メモリが個別、ズムが共通**。柔らかなアプリコット・セージ・ブラウンへ調整。素材は `assets/brand/`、共通部品の書き出しは `scripts/export-wordmarks.cjs`、方針は `docs/brand.md`。
- 名称・ロゴ更新 `a2d3d22` は [GitHub Pages run 35350213833](https://github.com/gerupon-lgtm/koekit/actions/runs/35350213833) で公開成功。公開13ファイル（ページ・CSS・manifest・SW・ロゴ・アイコン）が手元と一致することを確認。次作候補「ヌリリズム」は `docs/ideas.md` に発案者の回答を記録（3×3、声の相対移動、マスとパレットのタッチ対応、少数色、画像共有）。新作実装は未着手。

- 公開確認：UI変更 `c784cfd` は GitHub Pages [run 35348322274](https://github.com/gerupon-lgtm/koekit/actions/runs/35348322274) で成功。公開URLのHTML・コントローラ・CSS・開始案内・manifest・SWの9ファイルがローカルと一致することを確認済み。端末上の実発話確認は別途。

- 名称：器は「コエキット」、1本目は「ピタリズム」（旧：どうぶつめくり）、2本目は「メモリズム」（旧：きおくめくり）で2026-09-18に発案者が確定。既存の公開パスは維持。

- 一覧・両アプリのタイトル・盤面・操作ボタン・開始案内を共通配色で更新。開始案内を `src/ui/levelintro.js` へ共通化し、各コントローラの `LEVEL_INTRO` は置換済み。
- 「オーケー／オッケー」は既存語彙で認識可能と確認。案内の主表記を変更し、配置図に各レベルの位置語を表示。認識語・ゲームルールは変更していない。
- 詳細は `screens.md` 冒頭の現行UI記述を参照。GitHub Pagesへの反映には従来どおり stamp → commit → push が必要（公開反映はGitHub Actionsの成功と配信中のBUILD値で確認）。
- 検証：`TZ=UTC` の既存Nodeテスト120項目通過。Chromeの390×844／360×640表示、全13レベルの案内・戻る／Escape、きおくの選択→確定、どうぶつの同じ札を2回タップ→3回正解でクリア、選択時の盤面位置維持、動きを減らす設定を確認。ブラウザの未処理JS例外0件。音声の実発話・スマートフォン実機・オフラインは今回再検証していない。

- **コエキット** = 音声認識検証のPWA試作群。素のHTML/CSS/JS・ES Modules・**ビルド工程なし**。GitHub Pages 公開。
- 収録アプリは2本、どちらも**動作・公開済み**：
  - **ピタリズム**（`doubutsu/`）= 1本目。音声ルーレット（レベル0）＋位置語で当てる本編（レベル1〜5・延長）。
  - **メモリズム**（`kioku/`）= 2本目。9枚の絵を記憶→対象を探して位置を当てる。
- **フェーズ0の関門（要件11.1）は方式C=Voskで実機合格済み**（`docs/field-check-results.md`）。公開版はVoskで確定。
- 音声入力層は**方式差し替え可能な共通部品**（`src/speech/`）。これがこの案件の技術的な核。
- **前任のコードを作り直さない。** A区分は即変更可。C区分と確定5〜9は緩めない（`implementation-guide.md` 5節）。

- リポジトリ: https://github.com/gerupon-lgtm/koekit
- 公開: https://gerupon-lgtm.github.io/koekit/ （`/doubutsu/` `/kioku/`、音デモ `/demo/sounds.html`）

---

## 1. デプロイ手順（重要・毎回これ）

ビルドは無い。デプロイは **stamp → commit → push**。GitHub Pages が自動ビルドする。

```
node scripts/stamp-cache.cjs   # ← 必須。sw.js の BUILD を更新（キャッシュバスター）
git add -A && git commit -m "..." && git push
```

- **`stamp-cache.cjs` を実行し忘れると、アプリ本体のキャッシュが更新されない。** 各デプロイ前に必ず実行。
- ローカル確認は静的サーバで: `python -m http.server 8000`（または `npx serve .`）。**マイク・SWはHTTPSまたはlocalhostのみ。**
- 認証は `gh`（`gerupon-lgtm`）が通っている。push はそのまま可。`git` の LF→CRLF 警告は無害。

### キャッシュ方針（`sw.js`）
- **アプリ本体**（HTML/CSS/JS/フォント/アイコン）= network-first。オンラインなら常に最新。`APP_CACHE` 名に `BUILD` を含み毎デプロイ更新。
- **大きい静的資産**（`lib/vosk/vosk.js`、`assets/animals/`）= cache-first（`STATIC_CACHE = koekit-static-v1`）。毎デプロイでは再取得しない。**これらを差し替えたら `STATIC_CACHE` の版を上げる**（`sw.js`）。
- **Voskモデル（R2・別オリジン・約48MB）** = SW非対象。vosk-browser 側がキャッシュ。

---

## 2. テスト

決定的ロジックは Node スクリプトで検証（実装AIに毎回推論させない方針・`implementation-guide.md` 10節）。

```
node scripts/test-logic.cjs         # 語彙照合/CSV・JST/metrics/区間/判定/フェイント/deal（120項目）
TZ=UTC node scripts/test-logic.cjs  # JST依存の混入検出
```

実機・ブラウザ確認が主。**音声の実発話・ルーレットのアニメ・オフライン起動・PWA単独起動は実機（Pixel 6a）でのみ確認**。

---

## 3. アーキテクチャ / 主要ファイル

```
index.html            コエキット一覧(S-01)。2アプリへ遷移
styles.css            共有デザイントークン＋共有UIコンポーネントのCSS
sw.js / sw-register.js  Service Worker（キャッシュバスター）
version.json          バージョン正典(v0.1.0)
manifest.json         PWA
doubutsu/  index.html + app.js   ピタリズム（コントローラ）
kioku/     index.html + app.js   メモリズム（コントローラ）
demo/sounds.html      合成音の試聴デモ
lib/vosk/vosk.js       vosk-browser 自前配置（唯一の依存・約5.8MB）
assets/animals/        動物画像11枚（差し替え=src/game/animals.js の ANIMAL_FILES）
assets/fonts/ icons/   M PLUS Rounded 1c サブセット、PWAアイコン
probe/                 実機判定用の使い捨てページ（field-check.md 手順）
src/                   共有モジュール（下記）
scripts/               test-logic.cjs / stamp-cache.cjs
```

### src/（共有モジュール。両アプリが使う。アプリ専用実装にしない）
- **音声入力層（核）**：`speech/index.js`（ファクトリ）・`webspeech.js`(方式A/B)・`vosk.js`(方式C)・`config.js`（R2モデルURL）・`vocabulary.js`（同義語表＝データ・照合）・`vosk-worklet.js`。IFは `implementation-guide.md` 6節。
- **ゲーム**：`game/phase.js`（受け付け区間ステートマシン＝常時認識にしない）・`roulette.js`（ルーレット/フォーカス移動・フェイント）・`judge.js`（成否・回数）・`levels.js`・`positions.js`（位置→グリッド/矢印）・`deal.js`（きおく配置）・`animals.js`（画像マニフェスト）。
- **ログ**：`log/recorder.js`（メモリのみ）・`csv.js`（全クォート・JST）・`metrics.js`（合格ライン判定・閾値1箇所）。
- **UI（共有ビュー）**：`ui/board.js`（盤面）・`certificate.js`（認定書）・`levelselect.js`・`micstate.js`。
- **音**：`audio/sfx.js`（正解/不正解/クリア/GO/カウントダウン/タイムアップ）・`cries.js`（合成鳴き声・乗り物音）。
- `util/emitter.js`。

各アプリの `app.js` は上記を組み合わせるコントローラ。**盤面描画・図示・2段階確定・認定書・レベル選択・マイク表示は `src/ui/*` に共通化済み**（重複させない）。

---

## 4. 守る設計（緩めない）

`implementation-guide.md` 5節・要件0章C を必読。要点：

- **C-1**：公開版で利用者の音声を外部送信しない（→方式Vosk/端末内。方式A=既定WebSpeechは検証専用）。
- **C-2**：タッチのみで全機能操作可（マイク未許可でも遊べる）。
- **C-3**：2段階確定（位置をマーク→確定）を省略しない。タッチはダブルタップ（同じ札の再タップ）で確定できるが、これも2操作。
- **C-4**：コロガリズム不変更。
- **確定5〜9**：常時認識にしない（区間ごと開始/停止）／ゲームオーバー廃止しない（5回中3回、易しいレベルは回数を下げるのは可＝B区分）／確定待ちで周囲音を拾う問題に対策を入れない／認識なしはカウントしない／**子ども向けゲーム画面は文字を使わない**（版・©・数字・保護者向けの説明を除く）。

### 実機検証で確定した実装事実（`field-check-results.md`）
1. Voskモデルは `model/` を一段かませて tar.gz 化（直下配置は KaldiRecognizer 生成失敗）。
2. クロスオリジン分離は不要（mini-coi・CORS入れない）。
3. モデルはCache Storageにキャッシュ。更新戦略が要る。
4. 音声は AudioWorklet → AudioBuffer(16kHz) を acceptWaveform に渡す。
5. モデルは Cloudflare R2 公開URLから取得（`src/speech/config.js` の `DEFAULT_MODEL_URL` に設定済み。実ブラウザで取得〜Recognizer生成まで確認済み）。

---

## 5. 完了状況

### フェーズ0（関門・T-001〜T-014）
**方式C=Voskで4指標を全通過し合格**（`field-check-results.md`「フェーズ0 合格ライン計測」）。骨組み・フォント・PWA/SW・入力層・語彙・区間・ルーレット・ログ・CSV・計測パネル(S-07)まで実装・本番確認済み。詳細は `tasks.md` の「フェーズ0 実装状況」。

### フェーズ1（T-015〜T-025）
盤面・フォーカス移動（フェイント）・図示・2段階確定・成否/回数・レベル遷移・延長・認定書・タッチ・レベル選択まで実装。`tasks.md`「フェーズ1 実装状況」。

### メモリズム（2本目）
要件 `docs/requirements-kioku.md`（v1.4）、設計 `docs/kioku-design.md`。記憶提示→対象提示→回答→判定、失敗時の全開示、スタート導入まで実装・本番確認済み。

### 共通UX（A区分・両アプリ）— `tasks.md` に記録
- 各試行は**スタートで始まる**（ピタリズム=スタート/ストップ、きおく=スタートで券面表示＋カウントダウン）。「つぎ」ボタンは無し（スタートが次への合図）。結果は少し見せてスタート待ちへ。
- 図示は**カード自体をマーク**（矢印は使わない）。タッチは**ダブルタップで直接めくる**。
- 確定ボタンは**常時表示・選択前は非活性**（レイアウトシフト防止）。コントロール行の高さ固定。
- **クリア演出**（メダルのポップイン＋後光＋星）。タイトルに**コエキット トップへ戻る導線**。
- **各レベルの最初に保護者向けの概要**（`LEVEL_INTRO`、子に教える用なので文字あり）。
- **ピタリズムレベル0**：ルーレットを**3回止めたらレベル1へ**自動遷移。
- 動物画像11枚を実装（`src/game/animals.js`）。**鳴き声はWeb Audioの正解音で代用**（発案者方針）。

---

## 6. 未決事項・次の候補（Codexへの申し送り）

| 項目 | 内容 | 参照 |
|---|---|---|
| 実機受入（フェーズ1） | 1-a〜1-i、特に**未就学児の被験者**での確認（T-031）。iPhone XR は副対象で結果記録 | 要件14.2 |
| 公開版で方式A封じ | **2026-09-20実装済み**。パネル・URL・保存設定・factoryを制限。詳細は本書冒頭（T-029） | 要件11.2 / tasks T-029 |
| オフライン実機 | 機内モードで起動・プレイ（アプリシェル/画像/モデル）の実機確認（T-028） | 要件12 |
| 正式名称 | **確定済み**：コエキット／ピタリズム／メモリズム。公開済みパス `doubutsu/` `kioku/` は維持 | 要件 R-01 / KR-01 |
| 公開判断 | どの状態でSIKUMI LABとして公開するか（R-04） | 要件17 |
| 音の実素材 | 踏切・目覚まし等の実在音は合成で再現困難→**録音サンプル方式**が有力。`assets/sounds/` に置いて `sfx`/`cries` の該当を差し替える下地を作る、が未着手 | 会話ログ |
| 画像の調整 | 枚数・差し替えは `ANIMAL_FILES` 更新＋`STATIC_CACHE`版上げ。大きさ/余白の実機調整 | — |
| 秒数など | 記憶/対象提示秒・フォーカス枠の保持時間・クリア回数・フェイント確率はB区分。実機で調整 | requirements-kioku 8.5 等 |

**【想定】の扱い**：`implementation-guide.md` 4節に残る【想定】のうち確定したものは、該当文書へ確定として書き4節から外すこと（モデル配信先は確定済みで削除済み）。

---

## 7. 既知の落とし穴

- **ルーレットのアニメは `requestAnimationFrame` 駆動**。バックグラウンドのタブ/非表示のプレビューでは rAF が止まり回らない（自動テストで回せないことがある）。実機・前面では動く。
- **vosk-browser は保守が止まっている**（最終公開が古い）。動かない場合は速やかに発案者へ報告（tasks T-008 注意）。
- **方式A/B/Cはブラウザ機能に依存**。方式B（端末内WebSpeech）は実機で ja-JP 非対応と判明済（将来対応すれば予備）。
- **静的資産（画像/vosk.js）を差し替えたら `STATIC_CACHE` の版を上げる**（cache-first のため古いものが残る）。
- 計測パネル(S-07)の合格ライン判定は**レベル0のログ**で行う（`renderPanel` が level '0' でフィルタ）。

---

## 8. 参照文書

| ファイル | 内容 |
|---|---|
| `AGENTS.md` | Codex 入口（最初に読む順） |
| `docs/implementation-guide.md` | 実装指示の正典（技術スタック・**5節=変更禁止**・6節=音声入力層IF・8節=TZ・9節=版・10節=スクリプト・11節=コマンド） |
| `docs/requirements.md` | ピタリズム 要件（**0章A/B/C**・機能ID F-001〜） |
| `docs/requirements-kioku.md` | メモリズム 要件（機能ID KM-・KR-要確認・改訂履歴v1.4） |
| `docs/data-model.md` | LogEntry・CSV・レベル定義・metrics |
| `docs/screens.md` | 画面・遷移・デザイントークン |
| `docs/tasks.md` | 実装順・完了条件・**実装状況/検証済みの事実**・トレーサビリティ |
| `docs/field-check.md` / `field-check-results.md` | 実機判定の手順 / 結果（フェーズ0合格の証跡） |
| `docs/kioku-design.md` | メモリズムの薄い設計メモ（共有化・新規モジュール・タスク） |
| `docs/archive/` | 旧版・上流構想メモ（履歴。公開リポジトリには push していない＝ローカル保管、`.gitignore`） |

## 2026-09-19 音声ルーレットの効果音

- レベル0の数字の回転にも、カード移動と同じ `playTick(interval)` を使用。
- 完全停止ごとに `playCorrect()` を鳴らす。3回目も同じ正解音に統一。
- 停止後は従来どおり700ms待って受付再開、3回目は1000ms待ってレベル1へ。正解音中の認識停止を維持。
- 回転音を有効にした状態での実発話精度は実機確認が必要。

## 2026-09-19 レベル紹介のスクロール解消

- 両作品：説明を白いカード、開始ボタン・音声案内をカード外に分離。重複した説明を短くし、発話手順を横並びに整理。
- 320×568、360×640、390×844、768×1024で全13レベルの案内・ボタン・音声案内が画面内に収まり、縦横スクロールがないことをChromeで確認。発話候補の文字の重なり・8pxの余白も再確認。

- 紹介見出しの自動フォーカスによる黒枠を除去し、上下2px・左右4pxの余白を追加し、見出しを中央揃えに変更。操作ボタンのフォーカス表示は維持。

## 2026-09-19 正解画像の先送り・消灯防止・音量

- 両作品：正解音終了待ち500ms後、`await_result_next` で「つぎ／オーケー／オッケー」または次へボタンを受付。既存の自動送り時間は維持。旧タイマーを解除し、二重遷移を防ぐ。不正解には適用しない。
- 共通 `src/ui/screenawake.js`：プレイ中のみ画面維持を要求。タイトル・非表示で解除、復帰時に再取得。非対応／拒否でもプレイ継続。端末の省電力設定等による解除はあり得る。
- 効果音全体の masterGain を0.22→0.35（約1.6倍）へ。回転音との相対バランスを維持。実機での聞こえ方・音声認識への干渉は要確認。
- 検証：Nodeロジック153件。Chromeの模擬音声で両作品の先送り、効果音中の入力無視、タイマーの二重発火なし、クリア遷移、画面維持の開始／解除を確認。消灯防止の実機長時間検証は未実施。
- API仕様参照：https://developer.mozilla.org/en-US/docs/Web/API/Screen_Wake_Lock_API

## 2026-09-19 メインページのロゴ配置

- 発案者が比較案A「すっきり中央」を選択。各カードの文字ロゴと説明を中央揃えに反映。
- 3作品の仮表示では390×844でページ高902px（58px超過）、430×932で974px。768×1024では収まる。ヌリリズム追加時に見出し・カード間の余白を調整して再確認する。未実装カードは公開しない。

## 2026-09-19 数字拡大・正解演出統一・途中終了

- ピタリズムの数字：30vw→min(60vw, 48svh, 360px)。スマホでは約2倍。
- メモリズムの正解も共通CardRevealで中央拡大460ms・3秒表示。音声／タッチで先送りできる。不正解は従来の全開示・2.8秒を維持。
- 両作品のプレイ左上と認定書に終了アイコン。全音声受付区間に「やめる／おわり」を追加。記憶提示・効果音中の認識停止は維持し、この間はタッチで終了可能。
- 終了時にRoulette.reset、効果音stopAll、タイマー・拡大画像・音声認識・画面維持を解除。数値ルーレットを途中終了した後にtickが続く既存問題も修正。
- ロジック171件通過。終了語の実発話精度は実機で要確認。

- 2026-09-19 メモリズム：タイムアップ（一斉裏返し）から対象画像表示までの待機を450ms→800msに延長。対象画像の表示時間自体は変更しない。
