# 記録・称号と順番モード 実装計画（2026-09-20）

仕様: `next-games-discussion.md` の発案者合意事項。素のES Modules、既存UI・音声入力層を再利用する。

## 第1段階：既存2作品

- [x] `src/game/progress.js` に作品・モード別の端末内記録、称号、全通常クリアによる解放を集約。`scripts/test-progress.mjs` で最後だけクリア・降格防止・破損保存・保存拒否・作品間分離を検証。
- [x] 共通レベル選択に内容、クリア印、スピード解放条件を表示。共通称号UIをタイトルと認定書に接続。ゲーム中は数字でレベル表示。未クリアは再挑戦またはタイトル。
- [x] 既存スピードは `extra` 1ステージを維持。順番モードのみ通常と同じ手数構成をもう一周する。
- [x] Node回帰・ブラウザでタッチ、模擬音声、狭い画面、再読み込みを検証。関連文書を更新し stamp → commit → push → 公開内容照合。

## 第2段階：メモリズム・順番

- [x] 出題列・回答列を扱う `src/game/sequence.js` と専用コントローラ `kioku/sequence.js` を追加。場所モードは維持し、タイトルで入口を分ける。
- [x] 9マス固定、2〜6手、通常800/400ms・スピード600/300ms、最大3問中2正解。2問不正解でも3問目まで出す（既存Judgeに合わせた初期仕様）。
- [x] 動物を常時表示、問題間で再配置。位置ごとに追加、反復を保持、末尾取消・全消去、必要数のみ確定。全消去は配置と問題を保持。
- [x] 結果は番号付き静止表示、次へは手動。失敗トーストは900ms後に消し、正解番号を表示。音声「つぎ／オッケー」とタッチを共用。
- [x] 設定画面は後続。上限・速度は設定オブジェクトへ分離し最大9手を扱えるロジックにする。既得スピード称号のマーカーを保持。上限変更時のスピード記録の詳細は設定画面の設計時に決める。
- [x] ロジックとブラウザ結合を検証し、文書更新・デプロイ。陣地対戦は変更しない。

## 保存と既存変更

`koekit.progress.v1.<作品・モード>` に正規化したクリアIDを保存。音声や個人名は保存しない。過去のクリアは未記録なので初回は未獲得。保存が拒否された場合はセッション中だけ保持し、タイトルで知らせる。
作業前の `src/speech/vosk.js`、`docs/ideas.md`、`docs/tasks.md` の差分は保持し、無関係な差分はコミット対象に含めない。
