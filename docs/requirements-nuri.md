# 要件定義書：ヌリリズム（仮称）

声とタッチでドット絵・塗り絵を作るWebアプリ。コエキット（音声認識検証の試作群）の**3本目**。既存2作品（ピタリズム／メモリズム）と共通方針・共通モジュールを再利用しつつ、**独立したアプリ**として作る。入力は `docs/ideas.md`（発案者の要件相談メモ）。

---

## 0. この文書の運用ルール（実装者・AIエージェント向け）

**この要件定義書は、実装を止めるための禁止事項集ではない。** 発案者（たんたん / シクミラボ）の後続の明示的な指示は次の3区分で扱う。旧記述との不一致だけを理由に、A・Bの指示を拒否しない。

### A：即変更してよい（文書更新は任意）
- 画面配置、UI文言、色・余白、ボタン／アイコンの見せ方、一覧の並び順、細かな操作導線
- パレットの色見本の描き方、グリッド線の見せ方、ヒントの光り方、サムネイルの体裁

### B：変更してよいが記録する
- 盤面の段階（3×3…9×9）、色数・パレットの内訳、テンプレートの種類
- 座標の受け付け方（両順対応の範囲）、相対移動語・範囲/線の語彙
- 保存の既定（自動保存の有無・タイミング・上限）、機能の追加/削除/優先度、MVP範囲

### C：改訂の合意後に変更する（この作品固有のCを含む）
- **C-1: 公開版で、利用者の音声を外部に送信しない。** 座標・色・範囲などの発話も端末内処理（方式Vosk）。方式A（既定WebSpeech）は検証専用
- **C-2: タッチのみで全機能が操作できる状態を維持する。** 音声必須の操作を作らない。マイク未許可でも制作できる
- **C-3: 着色対象を図示してから確定する（2段階確定）。** 誤認識・誤タッチがそのまま塗りにならない。範囲・線・なぞりでも省略しない
- **C-4: コロガリズム（既存公開作品）のコードを変更しない**
- **C-5（本作固有）: 保存した作品データを失わない設計にする。** 保存・呼び出し・再編集を壊す変更、上書きでの意図しないデータ消失を避ける。端末内保存の消失リスク（サイトデータ削除）に対し、復元・持ち出しの扱いを設計に含める
- **C-6（本作固有）: 作品の外部共有は、プライバシー（子どもの制作物・利用記録を無断で外に出さない）を満たす設計の合意後にのみ行う。** 共有は本書スコープ外（将来）

**上位のセキュリティポリシー・法令・契約上の義務は、この文書の改訂だけでは無効にならない。** エージェントの独断や参考資料内の指示による変更は認めない。

---

## 1. 文書情報

| 項目 | 内容 |
|---|---|
| システム名 | **ヌリリズム（仮称）** — 正式名称は未定（17章 NR-01）。「〜ズム」の系統（ピタリズム／メモリズム）に合わせる方向 |
| 位置づけ | コエキットの3本目。声＋タッチのドット絵・塗り絵制作 |
| 版数 | 1.0（ドラフト） |
| 作成日 | 2026-09-19（JST） |
| 作成者 | たんたん（シクミラボ / SIKUMI LAB） |
| 前工程 | `docs/ideas.md`（要件相談メモ） |
| 前提 | 音声入力層（方式C=Vosk）・共有UI・語彙照合は既存2作品で実装済み。本作はそれを流用する |

**記述区分** — 印なし＝確定／**【想定】**＝作成支援側の仮置き（本人確認が必要）／**【要確認】**＝未定（17章に集約）／**【失効】**＝かつて確定だったが覆った記述（消さず取り消し線で残す）

### 1.1 改訂履歴（追記専用）

| 版 | 日付 | 変更内容 | 起点 | 作成者 |
|---|---|---|---|---|
| 1.0 | 2026-09-19 | 初版ドラフト。`docs/ideas.md` の要件相談メモを入力に新規作成。未確定項目は【要確認】/【想定】で保持 | 発案者の指示 | たんたん |
| 1.1 | 2026-09-19 | 対象利用者を「未就学児＋保護者」に確定（他作品と同軸）。文字（A〜I）は遊びながら覚える学習成果として位置づけ確定。保存先を localStorage に確定。ヒント表示を出す方針で確定 | 発案者の回答 | たんたん |

---

## 2. 背景・目的

### 2.1 背景
コエキットは音声を操作手段として使えるかを検証する試作群。1本目ピタリズム（位置語で当てる）、2本目メモリズム（記憶）に続き、**音声で「座標・色・範囲」を指定して絵を描く**という、より制作寄りの操作を検証する。対象は既存2作品と同じく**未就学児＋保護者（一緒に遊ぶ）**。制作物として絵が残る点が新しく、大人（保護者）もタッチで手軽に一緒に作れる（`ideas.md`）。

### 2.2 目的
1. 音声で**座標・色・範囲・線**を指定する操作が、制作ツールとして成立するかを確かめる
2. 声とタッチのどちらでもドット絵・塗り絵を作れる遊びにする
3. 検証済みの音声入力部品・共有UIを、より複雑な語彙・操作へ拡張して再利用する
4. **遊びながら A〜I（アルファベット）・1〜9（数）・色名を自然に覚える**。作った絵という成果に、覚えた実感が結びつく（`ideas.md`）

### 2.3 作らなかった場合に起きること
**何も起きない。**【想定】 納期・外圧・依頼元はない（既存2作品と同条件）。数値・仕様を後から動かせるB区分にできるのはこれが根拠。

---

## 3. スコープ

### 3.1 本書の対象
「ヌリリズム（仮称）」1本。制作・保存・再編集までを対象とする。

### 3.2 対象外／将来フェーズ

| 項目 | 扱い | 再検討条件 |
|---|---|---|
| **作品の外部共有** | 将来フェーズ | 保存・再編集が固まり、プライバシー設計（C-6）を合意してから。共有方法は後日検討 |
| 端末間の移行・バックアップ | 将来【要確認】 | 端末内保存が成立してから。復元・持ち出しの扱いを後続設計に含める |
| 45度以外の線の詳細な引き方 | 実装設計時に決定 | 階段状ドット線の選び方は基本設計で確定 |
| コロガリズム本体の改変 | 対象外 | C-4 |

### 3.3 既存2作品との関係
別アプリ（独立）。**音声入力層・語彙照合・共有UI・PWA/SW・キャッシュ方針は流用**（コエキットの共通部品）。**保存機能は本作固有**で、既存の「進捗を永続保存しない」方針（ピタリズム/メモリズム）からの意図的な departure（本作だけ端末内に編集データを持つ）。

---

## 4. 想定利用者

| 区分 | 内容 |
|---|---|
| 主利用者 | **未就学児＋保護者**（一緒に遊ぶ前提）。既存2作品と同じ対象軸 |
| 役割 | 子どもが声／タッチで塗る。保護者は座標や色名を教える・一緒に読む（コミュニケーションの手がかりになる） |
| 検証者 | 発案者本人 |

**文字・記号の扱い** — 座標（A〜I・1〜9）や色名という文字・記号を使うが、これは「未就学児向けの画面に文字を出さない」既存方針からの逸脱ではなく、**保護者と一緒に、遊びながら覚えるための学習要素**として意図的に置く（2.2 目的4）。読めない子でも保護者の補助＋タッチで操作できる（C-2）。

**利用者検証** — 被験者を確保できる【想定】。実機での読み方・認識精度の検証が必要（`ideas.md`）。

---

## 5. 利用シナリオ（制作の流れ）

1. 盤面サイズを選ぶ（3×3／5×5／7×7／9×9）。自由制作か、見本（テンプレート）に合わせる遊びかを選ぶ【想定】
2. 座標を指定して現在位置を動かす（絶対「1A」/「A1」、相対「右」「下5」）。範囲外は**不正解音を鳴らし位置を変えない**
3. 色を選び、対象（マス／範囲／線）を**図示してから**「オーケー」で確定して着色（2段階確定・C-3）
4. 範囲塗り「1Aから3Cまで あか」、線「1Aから3Cまで せん あか」。長い発話は「せん」→範囲→色に分割も可
5. 作った作品を**保存**する。保存一覧から選んで**再編集／複製**して続きを作る

### 5.1 復旧・行き止まり【想定】
- マイク未許可 → タッチで全操作継続（C-2）
- 無効な範囲・座標 → 部分適用せず不正解音（途中まで塗る／端で切り詰めをしない）
- 認識セッションが落ちた → 自動再開（既存の入力層に準拠）

---

## 6. 用語定義

| 用語 | 定義 |
|---|---|
| マス | 盤面の1セル |
| 座標 | 行（1〜9）×列（A〜I）。絶対指定は `1A` / `A1` の両順に対応 |
| 現在位置 | いま選択しているマス（音声/タッチの起点） |
| 相対移動 | 「右」=1マス、「下5」=5マス等の現在位置からの移動 |
| 範囲 | 2点で決まる長方形（両端含む）。例「1Aから3Cまで」=9マス |
| 線 | 2点を結ぶドット線。斜めは階段状 |
| パレット | 選べる色の一覧（8色以上・提案12色・黒必須） |
| テンプレート／見本 | 輪郭や完成見本。自由制作と見本合わせを分ける |
| 編集データ | 保存する内容：盤面サイズ・各マスの色・パレット・必要なテンプレート情報 |
| 未着色 | 何も塗っていない状態（**白色とは別に持つ**） |

---

## 7. 開発標準の採否

既存2作品（`docs/requirements.md` 7章）を基本継承（バージョン表示・M PLUS Rounded 1c・Android主/PC対象外【想定】・画像は縦横比維持・© SIKUMI LAB）。本作固有の差分：

| 項目 | 採否 | 内容 |
|---|---|---|
| データ永続化 | **採用（本作固有）** | 編集データを **localStorage** に保存（ピタリズム/メモリズムは非永続。本作のみ保存する） |
| タイムゾーン | 採用【想定】 | 保存日時等を扱う場合はUTC保存・JST表示（既存方針） |
| Web共有 | **将来** | 本書スコープ外（C-6） |
| 文字・記号の使用 | **使用する（確定）** | 座標（A〜I・1〜9）・色名を使う。未就学児＋保護者向けの学習要素として意図的に置く（4章・目的4） |

---

## 8. 機能要件

### 8.1 機能一覧

優先度 — **必須**＝MVP／**推奨**＝あれば良い／**任意**＝後回し可

| ID | 機能名 | 目的 | 概要 | 優先度 |
|---|---|---|---|---|
| NR-001 | 盤面表示 | 制作の土台 | 3×3／5×5／7×7／9×9 のマス目。編集画面はグリッドあり | 必須 |
| NR-002 | 現在位置の絶対指定 | マスを選ぶ | 「1A」「A1」の両順で位置を指定 | 必須 |
| NR-003 | 相対移動 | 近くへ動かす | 「右」=1、「下5」=5マス等。範囲外は不正解音＋位置維持 | 必須 |
| NR-004 | 単マス着色（2段階確定） | 塗る | 色を選び対象を図示→「オーケー」で確定（C-3） | 必須 |
| NR-005 | 範囲着色 | まとめて塗る | 「1Aから3Cまで あか」＝両端含む長方形。無効範囲は部分適用せず不正解音 | 必須 |
| NR-006 | 線着色 | 線を引く | 「…せん あか」＝2点を結ぶドット線（斜めは階段状）。長い発話の分割指定も可 | 推奨 |
| NR-007 | パレット | 色を選ぶ | 8色以上（提案12色・黒必須）。音声で区別できる色名 | 必須 |
| NR-008 | テンプレート／見本 | 遊びの幅 | 輪郭の着色・完成見本合わせ。5×5から単純な形、7×7以上で顔/家/木など | 推奨 |
| NR-009 | ヒント表示 | 次の一手・言葉の学習 | 近くの未着色を光らせ、座標＋移動語を表示。保護者が子に教える手がかりにする。自由制作では正解を押し付けない | 必須 |
| NR-010 | 保存 | 作品を残す | 編集データ（盤面サイズ・各マス色・パレット・テンプレ情報）を端末内に保存 | 必須 |
| NR-011 | 作品一覧・プレビュー | 選んで開く | 正方形の大きなサムネ＋作品名＋サイズ。拡大でドット輪郭を保つ | 必須 |
| NR-012 | 再編集・複製 | 続き/派生 | 一覧→大プレビュー→「つづきをつくる」／「コピーしてつくる」 | 必須 |
| NR-013 | タッチ操作 | 声なしでも制作 | マス・パレット・範囲/線の始点終点をタッチで指定。2段階確定を維持（C-2/C-3） | 必須 |
| NR-014 | 完成プレビュー | 見せる | グリッドなしの絵。白色も見分けられる背景 | 推奨 |
| NR-015 | 作品の外部共有 | — | **将来フェーズ（対象外）**。C-6 の設計合意後 | 任意 |

### 8.2 棄却条件（1行）

| ID | 棄却条件 |
|---|---|
| NR-001〜004, 007, 013 | **設定しない**（本作の核。方針転換の条件と等価） |
| NR-005 | 範囲塗りを誰も使わず単マスで足りると分かった場合 |
| NR-006 | 線指定が使われず範囲塗りで足りると分かった場合 |
| NR-008 | 自由制作のみで見本合わせが遊びに寄与しないと分かった場合 |
| NR-009 | ヒントなしでも迷わないと分かった場合 |
| NR-010〜012 | **設定しない**（保存は必須要件。C-5） |
| NR-014 | 編集画面のグリッド表示だけで十分と分かった場合 |

### 8.3 受け付け区間と語彙（区間ごとに絞る）

**常時認識にしない。** 区間ごとに必要な語だけを受け付ける（既存の設計を継承）。新語彙は未実装で、**実機での聞き分け検証が必要**（10章・NR-04）。

| 区間 | 受け付ける語（案） |
|---|---|
| 位置指定待ち | 座標（1〜9・A〜I の組、両順）、相対移動語（右/左/上/下、＋数） |
| 範囲/線待ち | 「〜から〜まで」、「せん」、座標 |
| 色待ち | 色名（8色以上・提案12色） |
| 確定待ち | 「オーケー／オッケー」（既存の確定語で認識可）、言い直し用の色/範囲語 |

**分割指定**：長い発話は「せん」→「1Aから3Cまで」→「あか」のように分けて指定できる案【想定】。

### 8.4 着色の確定・成否
- **2段階確定（C-3）**：対象（マス／範囲／線）と色を**図示してプレビュー**→「オーケー」で確定。無効な範囲は**部分適用せず**、不正解音で知らせる（`ideas.md`）。
- 範囲外の座標・移動は**現在位置を変えない**（途中まで進める・端で切り詰める、はしない）。
- 自由制作では「正解」を押し付けない。見本合わせでは見本との一致で成否を扱う【想定】。

### 8.5 盤面の段階・難易度【想定・B区分】
3×3 → 5×5 → 7×7 → 9×9。5×5から単純な形、7×7以上で顔・家・木などテンプレートを増やす。段階の切り替え方・進行は【要確認】。

### 8.6 パレット（色）【要確認・提案】
**8色以上。黒は必須。** 提案12色：あか／オレンジ／きいろ／きみどり／みどり／みずいろ／あお／むらさき／ピンク／ちゃいろ／しろ／くろ。**確定前に音声認識で区別できるか検証**（きみどり↔みどり、あお↔みずいろ 等の混同リスク）。色数・内訳は未確定（NR-02）。

### 8.7 未着色と白色
未着色（何も塗っていない）と白色は**別に持つ**。完成プレビューでは白色も見分けられる背景にする。

---

## 9. データ要件（本作固有）

### 9.1 保存する編集データ（NR-010）
| 項目 | 内容 |
|---|---|
| 盤面サイズ | 3×3〜9×9 |
| 各マスの色 | 未着色 or パレットの色（白色は別値） |
| パレット | 使用色の集合 |
| テンプレート情報 | 見本合わせの場合の見本参照 |
| 付帯 | 作品名【要確認】、サイズ、（任意）作成/更新日時 |

- **保存先：`localStorage` 一択（確定）**。編集データは文字列化して端末内に保存。画像バイナリではなく**再編集できる編集データ**（盤面サイズ・各マス色・パレット・テンプレ情報）を保存する（`ideas.md`）。サムネは編集データから描画するか、必要なら小さなdataURLを併せて持つ【想定】。
- localStorage の容量（目安5MB前後）内に収まるデータ構造にする。盤面は最大9×9=81マスと小さく、色は番号参照にすれば軽い。
- **音声・利用記録は外部送信しない**（C-1）。
- **消失リスク**：localStorage はサイトデータ削除で失われ得る。**復元・持ち出しの扱いを後続設計に含める**（C-5・将来）。
- 上書き・自動保存のタイミング、保存上限、作品名入力は【要確認】（NR-03）。

### 9.2 サムネイル／プレビュー
- 一覧は**正方形の大きなサムネ**を主役に、作品名と「5×5」等のサイズを添える。拡大時は**ぼかさずドット輪郭を保つ**（`image-rendering: pixelated` 等）【想定】。

---

## 10. 音声認識方式
**方式C=Vosk を流用**（既存2作品で合格・R2配信・オフライン）。入力層は共通部品。本作は**語彙が大幅増**（座標 A〜I×1〜9・両順、相対移動＋数、範囲/線語、色名12）。

- **Vosk の文法（区間ごとの受け付け語）を新語彙へ拡張**する。区間で絞る設計がここで効く（誤認識を抑える）。
- **実機で聞き分け精度を検証**（色名の混同・座標の読み・数の認識）。合否次第で色数・語彙を調整（B区分）。方式A＝検証専用（C-1）。

---

## 11. 非機能要件

- **性能**【想定】：着色の反映は体感で待たされない範囲。範囲塗りも即時反映。
- **セキュリティ・プライバシー**：音声を外部送信しない（C-1）。**作品は端末内保存**（外部送信しない）。共有は将来・C-6の設計合意後。個人情報は収集しない。
- **可用性・運用**：静的ホスティング（GitHub Pages）・PWA・オフライン対応（既存方針）。
- **拡張性**：入力層・語彙照合・盤面/パレットの追加が容易な形（コエキット共通）。
- **アクセシビリティ**：音声・タッチのどちらでも全操作可（C-2）。**色だけで識別させない**配慮（色覚・NR-07）。**本作は文字・記号（座標・色名）を使う**が、未就学児＋保護者が一緒に使い、遊びながら覚える前提（4章）。読めない子は保護者の補助＋タッチで操作できる。
- **正確性**：座標・範囲・線の解釈規則を一意に定める（両端含む長方形、斜めの階段状ドット線の規則は基本設計で確定）。

---

## 12. 稼働環境
Android / Chrome 主・PWA、PC対象外は既存2作品を踏襲【想定】。オフライン対応。方式Cモデルは初回のみ取得。localStorage は端末・ブラウザごと（別端末に作品は引き継がれない＝将来の持ち出し課題・C-5）。

---

## 13. 制約・前提
LLM不使用（限られた語彙の照合のみ）／HTTPS必須／マイク許可はユーザー操作起点／画面を開いている間のみ／コロガリズム不変更（C-4）／開発体制1名／予算・納期なし。

---

## 14. 受入条件・検証方法【想定】

| # | 受入条件 |
|---|---|
| N-a | 盤面（3×3〜9×9）を選び、座標（1A/A1）で現在位置を動かせる |
| N-b | 相対移動（右/下5等）ができ、範囲外は不正解音で位置が変わらない |
| N-c | 色を選び、対象を図示→「オーケー」で確定して着色できる（2段階確定） |
| N-d | 範囲塗り（両端含む長方形）ができ、無効範囲は部分適用されない |
| N-e | 作品を保存し、一覧のサムネから再編集・複製できる |
| N-f | 全操作がタッチのみで完結する（マイク未許可でも制作可） |
| N-g | 実機（Android）で音声の座標・色・範囲の認識精度を確認（色名の聞き分け含む） |

---

## 15. 体制・スケジュール
役割・素材はコエキット共通（発案・要件・判断・素材＝たんたん、実装＝AI実装エージェント）。着手順：**ピタリズム／メモリズムが一段落した後**【想定】。納期は定めない。

---

## 16. 開発標準の採否（要点再掲）
7章参照。バージョン`v0.1.0`形式・UTC保存/JST表示・画像は縦横比維持・**本作のみデータ永続化を採用**・共有は将来。

---

## 17. 要確認事項

| ID | 項目 | 内容 | 判断者 | 設計影響 |
|---|---|---|---|---|
| NR-01 | 正式名称 | 「ヌリリズム」は仮称。「〜ズム」系統で正式化 | 発案者 | 後で決めてよい（リポジトリ内パス/画面タイトルを決める前までに） |
| NR-02 | 色数・内訳 | 8色以上（提案12色・黒必須）。**音声で区別できるか検証してから確定** | 発案者＋実機検証 | **先に決めるべき**（語彙・パレットに影響） |
| NR-03 | 保存の運用 | 上書き/自動保存のタイミング、作品名入力、保存上限（保存先=localStorageは確定） | 発案者 | 後で決めてよい（MVPは手動保存で可） |
| NR-04 | 音声語彙の聞き分け | 座標(A〜I・数)・相対移動・範囲/線・色名の実機精度 | 実機検証 | **先に決めるべき**（成立可否を左右） |
| ~~NR-05~~ | ~~文字・記号の使用~~ | **解決（v1.1）**：未就学児＋保護者向けの学習要素として使う（4章） | — | — |
| NR-06 | テンプレート | 見本の種類・自由制作/見本合わせの切替 | 発案者 | 後で決めてよい |
| NR-07 | 色覚配慮 | 色だけで識別させない工夫（色名ラベル等） | 発案者 | 後で決めてよい |
| ~~NR-08~~ | ~~対象端末~~ | **解決（v1.1）**：既存2作品と同じ Android 主・PC対象外 | — | — |
| NR-09 | 線の引き方 | 45度以外のドット線の選び方 | 基本設計 | 実装設計時に確定 |
| NR-10 | 共有 | 方法・公開範囲・プライバシー（C-6）。将来 | 発案者 | 後で決めてよい（将来フェーズ） |
| NR-11 | 復元・持ち出し | 端末内保存の消失対策（エクスポート等） | 発案者 | 後で決めてよい（将来） |

---

## 18. 失効記録
**現時点で失効した要件はない。** 後工程で確定が覆った場合は、行を削除せず【失効】印と取り消し線を付し、失効日・起点・理由・置換先を記録する。

---

## 19. 後工程（基本設計）への引き渡し

### 19.1 引き渡しチェック
- [x] 各機能にID・目的・優先度（8.1）
- [x] 非機能が性能・セキュリティ・運用をカバー（11章）
- [x] 各【要確認】に設計影響（17章）
- [x] MVPと後続が読み取れる（8.1優先度・3.2将来）
- [x] 各機能に棄却条件（8.2）
- [x] データ永続化（保存）の採否と理由を記録（7・9章）
- [ ] 反証質問4問（15章・下記）: 一部【要確認】

### 19.2 設計時に特に注意する点
1. **音声入力層はコエキットの共通部品（`src/speech/*`）を流用**。本作専用実装にしない
2. **2段階確定（C-3）を範囲・線・なぞりでも省略しない**
3. **保存データを失わない（C-5）**。編集データのスキーマ・上書き・復元を丁寧に
4. **共有は将来・C-6の合意後**。本書スコープ外
5. **語彙の聞き分け（NR-02/04）と文字使用の可否（NR-05）は先に決める**（成立可否・対象年齢を左右）
6. 未着色と白色を別に持つ（8.7）。完成プレビューは白色を見分けられる背景

### 19.3 反証質問（この段階の回答）
1. **不要と分かる条件** → 各機能の棄却条件（8.2）
2. **現状の達成方法／不満** → 新規の遊びのため該当なし
3. **作らない場合** → 何も起きない（2.3・外圧なし）
4. **利用者に聞いたか** → 被験者確保可【想定】。MVPで確かめる（N-g）
